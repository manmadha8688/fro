# OAuth Client ID: put it in .env.local (see comment there) so it is not overwritten.
# Do not set VITE_GOOGLE_CLIENT_ID= empty here — it would override .env.local.

VITE_USE_MOCK=false
VITE_API_BASE_URL=/api
